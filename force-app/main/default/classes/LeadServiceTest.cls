public with sharing class LeadServiceTest {
    public LeadServiceTest() {

        @isTest 
        static void UpdTriggerLeadStatus (){
            //given
        Lead testLead=new Lead( LeadSource=='Partner Referral');
            //when
        test.startTest()
            //then
        }
    }
}
